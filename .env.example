# Keenetic (SSH и CLI)
ROUTER_HOST=192.168.1.1
ROUTER_USER=admin
ROUTER_PASSWORD=change-me

# VPN interface for new lists (dns-proxy route)
ROUTER_DNS_ROUTE_INTERFACE=Wireguard0

# Pause between CLI commands sent to router (seconds)
ROUTER_COMMAND_DELAY=0.1

# Git
GIT_BRANCH=main
CHECK_INTERVAL=60
